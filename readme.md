# Quantum Mechanical Keyboard Firmware

Alan's fork

To do: 
nothing for now; recently implemented all changes (num block instead of home row, gaming layer, moved delete (middle 2 fingers on bottom row, same for left and right hand))
